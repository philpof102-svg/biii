{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.philpof102-svg/biii",
  "description": "Safe-to-pay & token-genuineness verdicts on Base. Fail-closed, non-custodial: is a token the genuine issuer's or a look-alike? is a wallet known-bad? Every verdict re-verifiable on-chain.",
  "status": "active",
  "repository": {
    "url": "https://github.com/philpof102-svg/biii",
    "source": "github"
  },
  "version": "0.1.0",
  "_publishing_note": "This manifest declared an npm package `biii-mcp@0.1.0` that DOES NOT EXIST — registry.npmjs.org returns 404 for it. Submitted as written, the registry entry would have pointed at nothing: a manifest true in its shape and false at install time. The hosted endpoint below, by contrast, was probed and answers with a live tool list, so the remote form is the version that is true today. Restore a `packages` entry only after `npm view biii-mcp` actually resolves. Re-run scripts/listing-manifest.js before any submission: it reads the running server rather than this file, and it exits non-zero on deploy drift.",
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://biii-production.up.railway.app/mcp"
    }
  ]
}
