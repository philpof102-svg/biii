{
  "name": "biii",
  "version": "0.1.0",
  "private": true,
  "description": "The real-world USDC till on Base — the Square/SumUp of USDC: an independent merchant accepts USDC in person with just a phone. Non-custodial (their own wallet), no terminal, no PSP, universal EIP-681 QR, field-for-field on-chain verification, receipt anchored to the tx.",
  "type": "commonjs",
  "main": "lib/till.js",
  "scripts": {
    "test": "node test/till.test.js && node test/chain.test.js && node test/server.test.js && node test/ledger.test.js && node test/trust.test.js && node test/invoice.test.js && node test/asset.test.js && node test/harden.test.js && node test/rwa-registry.test.js && node eval/verdict-harness.js --check",
    "eval": "node eval/verdict-harness.js",
    "serve": "node lib/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "biii-mcp": "bin/biii-mcp.js"
  }
}
